# Spotify-Clone
A basic front end project that helped in value addition of my skills in web development

## Regenerate songs catalog
After adding/removing `.mp3` files in `songs/*`, run:

```bash
node scripts/generate-catalog.js
```
